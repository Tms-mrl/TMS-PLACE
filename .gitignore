node_modules/
dist/
.wrangler/
.dev.vars
*.log
.claude/scheduled_tasks.lock
.claude/settings.local.json
# `wrangler r2 object get` sin --pipe escribe el objeto a ./<key> al probar en local
/prop/

# Cache/estado de trabajo del importador de El Muelle (scripts/import-elmuelle.mjs).
# listings.json y el HTML cacheado son regenerables con `scrape`; el .sql final que
# sí se versiona vive en db/seed/elmuelle-import.sql.
scripts/.import-elmuelle/

# Token de la CLI de wrangler (CLOUDFLARE_API_TOKEN) para deployar sin login interactivo.
# Vence 2026-09-30 — borrar el archivo en esa fecha (o antes, revocado desde el dashboard).
.cf-deploy-token
